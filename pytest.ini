[pytest]
# 프로젝트 루트를 PYTHONPATH에 추가
pythonpath = .

# 테스트 파일 위치
testpaths = src/unit_tests

# 기본 옵션
addopts = -v

# 마커 등록
markers =
    integration: 실제 외부 서비스(Google API 등) 연동 테스트
    slow: 오래 걸리는 테스트
